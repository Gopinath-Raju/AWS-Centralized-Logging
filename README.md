# AWS-Centralized-Logging
Centralized AWS logging and monitoring system using CloudTrail, CloudWatch, and SNS. Captures all AWS API activity, detects Console login events via metric filters, triggers real-time CloudWatch alarms, and delivers email alerts through SNS. Audit logs stored in S3 for long-term retention.
